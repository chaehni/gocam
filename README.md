A wrapper for the Raspberry Pi camera module written in Go.
It makes use of the 'raspistill' and 'raspivid' commands for capturing pictures and videos.
For converting videos to .mp4 it uses the 'GPAC' package.
